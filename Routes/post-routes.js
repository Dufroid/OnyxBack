const Express = require("express");
const router = Express.Router();

// const {
//   postVideo,
//   searchStreams,
// } = require("../Controllers/post-controller");
// const { auth, setVideoStream } = require("../Middleware/auth");

// router.post(
//   "/postVideo/:id",
//   setVideoStream.single("UploadphotoInput"),
//   postVideo
// );
// router.get("/searchStreams/:id", auth, searchStreams);

module.exports = router;